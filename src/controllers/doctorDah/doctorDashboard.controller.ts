import { Request, Response } from "express";
import { ILoginRequest } from "../../interfaces/requests.interfaces";

import { ILoginResponse } from "../../interfaces/responses.interfaces";

export default async function doctorDashboardController(req: Request, res: Response) {
    /* if (req.session.user){
        console.log("yaaaaay". req.session.user.email)
        res.send('yaaaaay')

    }
    else{
    res.send('welp')
    } */
}
